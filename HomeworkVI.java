import java.util.*;
public class HomeworkVI
{
   public static String numberFreq()
   {
      //generate 1000 random integers in the range 0-10, inclusive
      //counts how many occurrences of each are entered
      //employ an array to store the frequencies
      //returns this array
      //NO other arrays should be created
      Random r=new Random();
      int[] list=new int[999];
      for(int i=0;i<999;i++)
      {
         list[i]=r.nextInt()+11;
      }
      for(int t=0;t<999;t++)
      {
         return "list[i]";
      }
   }
}